/**
*
* {{ properCase name }}
*
*/

{{#if memo}}
import React, { memo } from 'react';
{{else}}
import React from 'react';
{{/if}}
{{#if wantCreateSlice}}
import { useSelector, useDispatch } from "react-redux";
import { select{{properCase name}} } from "./slice";
{{/if}}
{{#if wantStyle}}
import { {{properCase name}}Container } from './styles';
{{/if}}
// import PropTypes from 'prop-types';

function {{ properCase name }}() {
{{#if wantCreateSlice}}
const {{name}}State = useSelector(select{{properCase name}});
const dispatch = useDispatch();
{{/if}}
return (
<{{properCase name}}Container>
  {{ properCase name }} Features
</{{properCase name}}Container>
);
}

// {{ properCase name }}.propTypes = {};

{{#if memo}}
export default memo({{ properCase name }});
{{else}}
export default {{ properCase name }};
{{/if}}